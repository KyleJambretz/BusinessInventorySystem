<Window x:Class="BusinessInventorySystem.Views.AdjustStockDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Adjust Stock" Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource WindowBackgroundBrush}">
    <StackPanel Margin="20">
        <TextBlock Text="Adjust Stock" Style="{StaticResource PageTitle}" Margin="0,0,0,16"/>

        <TextBlock Text="ITEM" Style="{StaticResource MutedText}" Margin="0,0,0,4"/>
        <ComboBox x:Name="ItemCombo" Height="34" VerticalContentAlignment="Center" Margin="0,0,0,4"
                  SelectionChanged="ItemCombo_SelectionChanged">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock>
                        <Run Text="{Binding Sku, Mode=OneWay}"/> — <Run Text="{Binding Name, Mode=OneWay}"/>
                    </TextBlock>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <TextBlock x:Name="OnHandText" Style="{StaticResource MutedText}" Margin="0,0,0,10"/>

        <UniformGrid Columns="2" Margin="0,0,0,10">
            <StackPanel Margin="0,0,8,0">
                <TextBlock Text="TYPE" Style="{StaticResource MutedText}" Margin="0,0,0,4"/>
                <ComboBox x:Name="TypeCombo" Height="34" VerticalContentAlignment="Center"/>
            </StackPanel>
            <StackPanel>
                <TextBlock Text="QUANTITY CHANGE" Style="{StaticResource MutedText}" Margin="0,0,0,4"/>
                <TextBox x:Name="ChangeBox" Style="{StaticResource InputBox}"/>
            </StackPanel>
        </UniformGrid>
        <TextBlock Text="Use a negative number to remove stock (e.g. -5 for a sale)."
                   Style="{StaticResource MutedText}" Margin="0,0,0,10"/>

        <TextBlock Text="NOTE" Style="{StaticResource MutedText}" Margin="0,0,0,4"/>
        <TextBox x:Name="NoteBox" Style="{StaticResource InputBox}" Margin="0,0,0,16"/>

        <TextBlock x:Name="ErrorText" Foreground="{StaticResource DangerBrush}" FontSize="12"
                   TextWrapping="Wrap" Margin="0,0,0,10" Visibility="Collapsed"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0"
                    IsCancel="True"/>
            <Button Content="Apply" Style="{StaticResource PrimaryButton}" IsDefault="True"
                    Click="Apply_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
