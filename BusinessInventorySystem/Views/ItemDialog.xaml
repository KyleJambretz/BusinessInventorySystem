<Window x:Class="BusinessInventorySystem.Views.ItemDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Item" Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource WindowBackgroundBrush}">
    <StackPanel Margin="20">
        <TextBlock x:Name="TitleText" Text="Add Item" Style="{StaticResource PageTitle}" Margin="0,0,0,16"/>

        <TextBlock Text="SKU" Style="{StaticResource MutedText}" Margin="0,0,0,4"/>
        <TextBox x:Name="SkuBox" Style="{StaticResource InputBox}" Margin="0,0,0,10"/>

        <TextBlock Text="NAME" Style="{StaticResource MutedText}" Margin="0,0,0,4"/>
        <TextBox x:Name="NameBox" Style="{StaticResource InputBox}" Margin="0,0,0,10"/>

        <TextBlock Text="CATEGORY" Style="{StaticResource MutedText}" Margin="0,0,0,4"/>
        <TextBox x:Name="CategoryBox" Style="{StaticResource InputBox}" Margin="0,0,0,10"/>

        <UniformGrid Columns="3" Margin="0,0,0,10">
            <StackPanel Margin="0,0,8,0">
                <TextBlock Text="QUANTITY" Style="{StaticResource MutedText}" Margin="0,0,0,4"/>
                <TextBox x:Name="QuantityBox" Style="{StaticResource InputBox}" Text="0"/>
            </StackPanel>
            <StackPanel Margin="0,0,8,0">
                <TextBlock Text="REORDER AT" Style="{StaticResource MutedText}" Margin="0,0,0,4"/>
                <TextBox x:Name="ReorderBox" Style="{StaticResource InputBox}" Text="0"/>
            </StackPanel>
            <StackPanel>
                <TextBlock Text="UNIT PRICE" Style="{StaticResource MutedText}" Margin="0,0,0,4"/>
                <TextBox x:Name="PriceBox" Style="{StaticResource InputBox}" Text="0.00"/>
            </StackPanel>
        </UniformGrid>

        <TextBlock Text="LOCATION" Style="{StaticResource MutedText}" Margin="0,0,0,4"/>
        <TextBox x:Name="LocationBox" Style="{StaticResource InputBox}" Margin="0,0,0,16"/>

        <TextBlock x:Name="ErrorText" Foreground="{StaticResource DangerBrush}" FontSize="12"
                   TextWrapping="Wrap" Margin="0,0,0,10" Visibility="Collapsed"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0"
                    IsCancel="True"/>
            <Button x:Name="SaveButton" Content="Add Item" Style="{StaticResource PrimaryButton}"
                    IsDefault="True" Click="Save_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
